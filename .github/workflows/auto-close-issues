name: Auto Close Issues

on:
  pull_request:
    types: [closed]

jobs:
  auto-close-issues:
    runs-on: ubuntu-latest
    steps:
      - name: Check if PR was merged
        if: github.event.pull_request.merged == true
        run: |
          echo "PR was merged, checking for issue references..."
